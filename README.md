# torchstack-linear-mlops
train a tiny PyTorch model on a synthetic dataset (generated with NumPy), training container, experiment tracking (MLflow), artifact saving, API service (FastAPI), and composing orchestration
